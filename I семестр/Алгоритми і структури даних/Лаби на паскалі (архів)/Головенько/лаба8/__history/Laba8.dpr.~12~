program Laba8;

{$APPTYPE CONSOLE}


uses
  Unit2 in 'Unit2.pas';

begin
  CreateMatrix(n, f, Matrix);
  WriteMatrix(n, Matrix);
  SortVector(n,Vector);
  CreateVector(n, Matrix, Vector);
  WriteVector(n, Vector);
  f(i,j);
end.
