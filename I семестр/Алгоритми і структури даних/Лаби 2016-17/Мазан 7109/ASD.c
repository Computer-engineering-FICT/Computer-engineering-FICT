#include <stdio.h>
/* –9x3  + 5x2, x ∈ (2,12] U (22,32) */
/*   – x2  – 12, x ∈ (– ∞, 0] */

int main()
{	
	float x, y;
	printf("Введіть x: ");
	scanf("%f", &x);
	if ((x > 2 && x <=12) || (x > 22 && x < 32)) {
		y = -9*x*x*x+5*x*x;
		printf ("%9f\n", y);
	}
	if (x <= 0) {
		y = -1*x*x-12;
		printf ("%9f\n", y);
	}
	if ((x > 0 && x <= 2) || (x > 12 && x <= 22)) {
		printf ("\nЗначення, які ви ввели, не дозволяють провести обрахунок\n");
	}
	
}
