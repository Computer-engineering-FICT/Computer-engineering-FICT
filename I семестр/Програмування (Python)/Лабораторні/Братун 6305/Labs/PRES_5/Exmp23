>>> obj = range(len([1, 2, 3]))
>>> obj
range(0, 3)
>>> obj[0],obj[1],obj[2] # Доступ по індексу

>>> obj[0:2] # Отримання зрізу

>>> i=iter(obj)
>>> next(i),next(i),next(i) # Доступ за допомоги ітераторів

>>> list(obj) # Перетворення діапазона на список

>>> 1 in obj, 7 in obj # Перевірка на входження значення
